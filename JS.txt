JAVA SCRIPT 

1) What is variable ?

Variable is like a container, we stored the values in it i.e we assign the values to variables with assignment operator(=).
When defining varibles we use let or const methods.
let : when ever we want we can update the values,and we can define a varible without values(undefined).
const : we deffenetly assign value to it,and we can modified the values.

2) What is DOM ?

DOM is document object model, it is a structured representation of HTML page created by browser. it allows js to manipulate,strucure,style 
to website
Document Object is entry point to access the HTML page.like body tag

3) What is DOM methods and properties ?

mehtods :
getElementById,createElementById,appendChild,classList.add,classList.remove

Properties : 
textContent,
style - color, fontFamaily,value

Events : onclick Event, 

4) What are types of values in js ?

Reference type
Primitive type : number, string, boolean, undefined.

5) What are Math function ?

Math.ceil, math.random

6) What are input Elements ?

 input element takes data from users
 types of inputs - Text, Password , Radio, Date, Checkbox 

7) what are comparision operator ?

comparing two values
(===) strict equal to - compares both values and typeof values 
(==) loose equal to - compares only values.

8) What are Data structures in JS ?

Arrays,Objects, Maps, Sets






